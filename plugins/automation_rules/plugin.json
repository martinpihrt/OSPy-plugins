{
  "schema_version": 1,
  "id": "automation_rules",
  "name": "Automation Rules",
  "menu": "Package: Automation Rules",
  "version": "1.1.4",
  "description": "Builds graphical AND/OR rules over cached provider and OSPy values, sends detailed notifications and optionally performs guarded control actions with incident locking.",
  "author": "Martin Pihrt",
  "license": "GPL-3.0",
  "ospy": {
    "min": "3.0.354"
  },
  "python": {
    "min": "3.8"
  },
  "mobile": {
    "api_version": 1,
    "actions": []
  },
  "dependencies": [
    {"id": "water_meter", "required": false},
    {"id": "pressure_monitor", "required": false},
    {"id": "tank_monitor", "required": false},
    {"id": "current_loop_tanks_monitor", "required": false},
    {"id": "venetian_blind", "required": false},
    {"id": "ospy_backup", "required": false},
    {"id": "email_notifications", "required": false},
    {"id": "email_notifications_ssl", "required": false},
    {"id": "telegram_bot", "required": false}
  ],
  "permissions": [
    "files",
    "email",
    "network",
    "system"
  ]
}
